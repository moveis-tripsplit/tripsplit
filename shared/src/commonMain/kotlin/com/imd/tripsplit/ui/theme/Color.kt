package com.imd.tripsplit.ui.theme

import androidx.compose.ui.graphics.Color

// ==========================================
// 🎨 TripSplit Design System - Cores Oficiais
// ==========================================

// --- Paleta Principal ---
val BlueOcean = Color(0xFF2563EB)      // Primary: Ações principais, links, navegação
val GreenTravel = Color(0xFF16A34A)    // Secondary: Sucesso, dinheiro positivo, saldo
val OrangeAccent = Color(0xFFF97316)   // Accent: Viagens, destaques

// --- Cores de Superfície (Light Mode) ---
val BackgroundLight = Color(0xFFF8FAFC) // Fundo geral das telas
val SurfaceLight = Color(0xFFFFFFFF)    // Fundo de cards, diálogos e containers
val BorderLight = Color(0xFFE2E8F0)     // Divisores e bordas de inputs

// --- Cores de Texto (Light Mode) ---
val TextPrimary = Color(0xFF172033)     // Texto principal (quase preto azulado)
val TextSecondary = Color(0xFF64748B)   // Textos auxiliares e placeholders
val TextDisabled = Color(0xFF94A3B8)    // Elementos desabilitados

// --- Cores Semânticas ---
val SuccessGreen = Color(0xFF16A34A)    // Positivo, concluído, pago
val ErrorRed = Color(0xFFDC2626)        // Dívidas, erros de validação
val WarningYellow = Color(0xFFEAB308)   // Alertas e pendências
val InfoBlue = Color(0xFF2563EB)        // Mensagens informativas

// --- Cores para Dark Mode (Suporte Futuro) ---
val BackgroundDark = Color(0xFF0F172A)
val SurfaceDark = Color(0xFF1E293B)
val TextPrimaryDark = Color(0xFFF8FAFC)
val TextSecondaryDark = Color(0xFFCBD5E1)
val BorderDark = Color(0xFF334155)
val PrimaryDark = Color(0xFF3B82F6)
val SuccessDark = Color(0xFF22C55E)
val ErrorDark = Color(0xFFEF4444)
val WarningDark = Color(0xFFFACC15)
